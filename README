Guessing game
 
We teach the app some basic types of animals and it tries to guess wich one we're talking about.

# API Overview #

while (!board.hasFinished()) {
    String question = board.move();
    board.play(answer);
}
board.hasVictory();
